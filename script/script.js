let displayEl = document.getElementById("display");
let buttonsEl = document.querySelectorAll("button");

let operatorEl = document.querySelectorAll(".operator");
let operandEl = document.querySelectorAll(".operand");


const buttons= buttonsEl.forEach(
    button=>{button.addEventListener}
)

function add(num1, num2){
    var result = 0;
    return result= num1 + num2;
}

console.log(add(operandEl.values,));